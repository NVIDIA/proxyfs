#
# build the commands and stuff
#
    cd proto
    go get
    go build
    go install		

    cd ../hacli
    go build
    go install

#
# setup the Samba config
#
# do this on all 3 nodes, or else setup parallel-ssh
# and do it once in the host OS
#
    sudo ln -s /vagrant/src/github.com/swiftstack/ProxyFS/consensus/proto/smb-myTestVg?.conf /etc/samba/.
    sudo mkdir -p /var/cache/samba/vg1 /var/cache/samba/vg2

# or do each command only once:
	
    parallel-ssh -h etc-hosts.txt sudo ln -s /vagrant/src/github.com/swiftstack/ProxyFS/consensus/proto/smb-myTestVg?.conf /etc/samba/.
    parallel-ssh -h etc-hosts.txt sudo mkdir -p /var/cache/samba /var/cache/samba
